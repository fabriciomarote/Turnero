# MongoDB (valores por defecto para desarrollo local; en Docker usar variables de entorno)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/turnero}

# CORS (para Docker: app.cors.allowed-origins=http://frontend:3000,http://localhost:3000)
app.cors.allowed-origins=http://localhost:3000
