# Copiar a .env en la raíz del repo (opcional). Usado por docker-compose.

# URL pública del API (la que usa el navegador). Debe coincidir con el puerto del servicio app.
API_PUBLIC_URL=http://localhost:8083

# Puerto en el host para la SPA (nginx sirve en 80 dentro del contenedor).
FRONT_PORT=3000
